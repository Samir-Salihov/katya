# Netlify: статический сайт, сборки нет — публикуем корень репозитория.
# base не указываем (репозиторий уже в корне), поэтому «Base directory»
# в настройках сайта должен быть ПУСТЫМ.
[build]
  publish = "."
